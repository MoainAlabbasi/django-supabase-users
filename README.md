# 🎯 نظام إدارة المستخدمين - Django + Supabase

مشروع Django بسيط لإدارة وتخزين أسماء المستخدمين مع الربط بقاعدة بيانات Supabase (PostgreSQL).

## 📋 المحتويات

- [المميزات](#-المميزات)
- [المتطلبات](#-المتطلبات)
- [خطوات إعداد قاعدة البيانات في Supabase](#-خطوات-إعداد-قاعدة-البيانات-في-supabase)
- [خطوات تشغيل المشروع محلياً](#-خطوات-تشغيل-المشروع-محلياً)
- [خطوات الرفع على GitHub](#-خطوات-الرفع-على-github)
- [ربط المشروع بـ Supabase](#-ربط-المشروع-بـ-supabase)
- [استخدام الموقع](#-استخدام-الموقع)

## ✨ المميزات

- ✅ إضافة مستخدمين جدد
- ✅ عرض قائمة المستخدمين
- ✅ حذف المستخدمين
- ✅ واجهة مستخدم جميلة واحترافية
- ✅ الربط بقاعدة بيانات Supabase (PostgreSQL)
- ✅ لوحة إدارة Django
- ✅ رسائل نجاح وخطأ تفاعلية

## 📦 المتطلبات

- Python 3.8 أو أحدث
- حساب في Supabase (مجاني)
- Git (للرفع على GitHub)

## 🗄️ خطوات إعداد قاعدة البيانات في Supabase

### 1. إنشاء المشروع

لقد أنشأت مشروع Supabase بالمعلومات التالية:
- **اسم المشروع**: test1
- **كلمة مرور قاعدة البيانات**: Ab8877@db
- **المنطقة**: آسيا والمحيط الهادئ

### 2. تنفيذ كود SQL

1. افتح لوحة تحكم Supabase: https://supabase.com/dashboard
2. اختر مشروعك `test1`
3. من القائمة الجانبية، اختر **SQL Editor**
4. انقر على **New Query**
5. انسخ محتوى ملف `supabase_setup.sql` والصقه في المحرر
6. انقر على **Run** لتنفيذ الكود

الكود سيقوم بـ:
- إنشاء جدول `users` لتخزين المستخدمين
- إضافة فهرس على اسم المستخدم
- إنشاء مشغل لتحديث وقت التعديل تلقائياً
- إضافة بيانات تجريبية (اختياري)

### 3. الحصول على معلومات الاتصال

1. من لوحة تحكم Supabase، اذهب إلى **Settings** → **Database**
2. في قسم **Connection String**، اختر **URI**
3. انسخ الرابط الذي يبدأ بـ `postgresql://postgres:[YOUR-PASSWORD]@db.xxx.supabase.co:5432/postgres`
4. استخرج منه:
   - **DB_HOST**: `db.xxx.supabase.co` (حيث xxx هو رمز مشروعك)
   - **DB_PORT**: `5432`
   - **DB_USER**: `postgres`
   - **DB_PASSWORD**: `Ab8877@db` (كلمة المرور التي أدخلتها)
   - **DB_NAME**: `postgres`

## 🚀 خطوات تشغيل المشروع محلياً

### 1. استنساخ المشروع

```bash
git clone <رابط-المستودع>
cd django_supabase_users
```

### 2. إنشاء بيئة افتراضية

```bash
python3 -m venv venv
```

### 3. تفعيل البيئة الافتراضية

**على Linux/Mac:**
```bash
source venv/bin/activate
```

**على Windows:**
```bash
venv\Scripts\activate
```

### 4. تثبيت المتطلبات

```bash
pip install -r requirements.txt
```

### 5. تكوين المتغيرات البيئية

1. افتح ملف `.env`
2. استبدل `YOUR_PROJECT_REF` برمز مشروعك من Supabase
3. تأكد من صحة جميع البيانات

مثال:
```env
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=Ab8877@db
DB_HOST=db.abcdefghijklmnop.supabase.co
DB_PORT=5432
```

### 6. إنشاء جداول Django الإدارية

```bash
python manage.py migrate
```

### 7. إنشاء مستخدم إداري

```bash
python manage.py createsuperuser
```

أدخل:
- اسم المستخدم
- البريد الإلكتروني (اختياري)
- كلمة المرور

### 8. تشغيل الخادم

```bash
python manage.py runserver
```

### 9. فتح الموقع

افتح المتصفح وانتقل إلى:
- **الصفحة الرئيسية**: http://127.0.0.1:8000/
- **لوحة الإدارة**: http://127.0.0.1:8000/admin/

## 📤 خطوات الرفع على GitHub

### 1. إنشاء مستودع جديد على GitHub

1. اذهب إلى https://github.com/new
2. أدخل اسم المستودع (مثل: `django-supabase-users`)
3. اختر **Public** أو **Private**
4. **لا تُفعّل** خيار "Initialize this repository with a README"
5. انقر على **Create repository**

### 2. ربط المشروع بـ GitHub

في مجلد المشروع، نفذ الأوامر التالية:

```bash
# تهيئة Git
git init

# إضافة جميع الملفات
git add .

# إنشاء أول commit
git commit -m "Initial commit: Django + Supabase user management system"

# ربط المستودع البعيد (استبدل USERNAME و REPO_NAME)
git remote add origin https://github.com/USERNAME/REPO_NAME.git

# رفع الكود
git branch -M main
git push -u origin main
```

### 3. التحقق من الرفع

1. افتح صفحة المستودع على GitHub
2. تأكد من ظهور جميع الملفات
3. تأكد من أن ملف `.env` **غير موجود** (لأنه في `.gitignore`)

## 🔗 ربط المشروع بـ Supabase

### طريقة الحصول على رمز المشروع (Project Reference ID)

1. افتح لوحة تحكم Supabase
2. اختر مشروعك `test1`
3. اذهب إلى **Settings** → **General**
4. ابحث عن **Reference ID** أو **Project URL**
5. الرمز سيكون في الرابط: `https://supabase.com/dashboard/project/YOUR_PROJECT_REF`

### تحديث ملف .env

```env
DB_HOST=db.YOUR_PROJECT_REF.supabase.co
```

استبدل `YOUR_PROJECT_REF` بالرمز الفعلي.

### اختبار الاتصال

```bash
python manage.py migrate
```

إذا نجح الأمر، فقد تم الربط بنجاح! ✅

## 🎨 استخدام الموقع

### الصفحة الرئيسية

- عرض قائمة جميع المستخدمين
- إحصائيات عدد المستخدمين
- أزرار للإضافة والحذف

### إضافة مستخدم

1. انقر على **إضافة مستخدم جديد**
2. أدخل اسم المستخدم (مطلوب)
3. أدخل البريد الإلكتروني (اختياري)
4. انقر على **حفظ المستخدم**

### حذف مستخدم

1. من قائمة المستخدمين، انقر على **حذف** بجانب المستخدم
2. أكد الحذف في النافذة المنبثقة

### لوحة الإدارة

1. اذهب إلى http://127.0.0.1:8000/admin/
2. سجل الدخول بحساب المدير
3. يمكنك إدارة المستخدمين من هنا أيضاً

## 📁 هيكل المشروع

```
django_supabase_users/
├── manage.py                 # ملف إدارة Django
├── requirements.txt          # المكتبات المطلوبة
├── .env                      # المتغيرات البيئية (لا يُرفع على GitHub)
├── .env.example             # مثال على المتغيرات البيئية
├── .gitignore               # الملفات المستثناة من Git
├── supabase_setup.sql       # كود SQL لإنشاء القاعدة
├── README.md                # هذا الملف
├── userproject/             # إعدادات المشروع
│   ├── settings.py          # إعدادات Django
│   ├── urls.py              # مسارات URL الرئيسية
│   └── wsgi.py              # إعدادات WSGI
├── users/                   # تطبيق المستخدمين
│   ├── models.py            # نماذج قاعدة البيانات
│   ├── views.py             # عرض الصفحات
│   ├── urls.py              # مسارات التطبيق
│   └── admin.py             # إعدادات لوحة الإدارة
└── templates/               # قوالب HTML
    ├── base.html            # القالب الأساسي
    └── users/
        ├── user_list.html   # صفحة قائمة المستخدمين
        └── add_user.html    # صفحة إضافة مستخدم
```

## 🔒 ملاحظات أمنية مهمة

⚠️ **تحذير**: لا ترفع ملف `.env` على GitHub أبداً!

- ملف `.env` يحتوي على معلومات حساسة (كلمات المرور، مفاتيح API)
- تأكد من وجود `.env` في ملف `.gitignore`
- استخدم `.env.example` كمرجع للآخرين

## 🐛 حل المشاكل الشائعة

### خطأ في الاتصال بقاعدة البيانات

```
django.db.utils.OperationalError: could not connect to server
```

**الحل**:
1. تحقق من صحة `DB_HOST` في ملف `.env`
2. تأكد من أن المشروع في Supabase نشط
3. تحقق من كلمة المرور

### خطأ "table does not exist"

```
django.db.utils.ProgrammingError: relation "users" does not exist
```

**الحل**:
1. تأكد من تنفيذ كود SQL في Supabase
2. نفذ `python manage.py migrate`

### خطأ في استيراد decouple

```
ModuleNotFoundError: No module named 'decouple'
```

**الحل**:
```bash
pip install python-decouple
```

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من قسم [حل المشاكل الشائعة](#-حل-المشاكل-الشائعة)
2. راجع [وثائق Django](https://docs.djangoproject.com/)
3. راجع [وثائق Supabase](https://supabase.com/docs)

## 📝 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

---

**تم التطوير بـ ❤️ باستخدام Django + Supabase**
