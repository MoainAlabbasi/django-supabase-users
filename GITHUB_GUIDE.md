# 📤 دليل رفع المشروع على GitHub

## المتطلبات

- حساب GitHub (إذا لم يكن لديك، أنشئ حساباً من https://github.com/signup)
- Git مثبت على جهازك

---

## الطريقة الأولى: عبر سطر الأوامر (Terminal/CMD)

### الخطوة 1: إنشاء مستودع جديد على GitHub

1. اذهب إلى https://github.com/new
2. املأ البيانات:
   - **Repository name**: `django-supabase-users` (أو أي اسم تريده)
   - **Description**: "نظام إدارة مستخدمين بسيط باستخدام Django + Supabase"
   - اختر **Public** (عام) أو **Private** (خاص)
   - **لا تُفعّل** خيار "Add a README file"
   - **لا تُفعّل** خيار "Add .gitignore"
3. انقر على **Create repository**

### الخطوة 2: ربط المشروع المحلي بـ GitHub

افتح Terminal/CMD في مجلد المشروع ونفذ الأوامر التالية:

```bash
# 1. تهيئة Git في المشروع
git init

# 2. إضافة جميع الملفات للتتبع
git add .

# 3. إنشاء أول commit
git commit -m "Initial commit: Django + Supabase user management system"

# 4. ربط المستودع البعيد
# استبدل YOUR_USERNAME باسم مستخدمك في GitHub
# استبدل YOUR_REPO باسم المستودع الذي أنشأته
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# 5. تسمية الفرع الرئيسي
git branch -M main

# 6. رفع الكود إلى GitHub
git push -u origin main
```

### الخطوة 3: إدخال بيانات الدخول

عند تنفيذ `git push`، سيطلب منك:
- **Username**: اسم مستخدمك في GitHub
- **Password**: استخدم **Personal Access Token** وليس كلمة المرور العادية

#### كيفية إنشاء Personal Access Token:

1. اذهب إلى https://github.com/settings/tokens
2. انقر على **Generate new token** → **Generate new token (classic)**
3. أدخل اسماً للتوكن (مثل: "Django Project")
4. اختر صلاحية **repo** (كامل الصلاحيات للمستودعات)
5. انقر على **Generate token**
6. **انسخ التوكن فوراً** (لن تراه مرة أخرى!)
7. استخدمه بدلاً من كلمة المرور عند الرفع

### الخطوة 4: التحقق من الرفع

1. افتح صفحة المستودع على GitHub
2. تأكد من ظهور جميع الملفات
3. تأكد من أن ملف `.env` **غير موجود** (لأنه في `.gitignore`)

✅ **تم بنجاح!** الآن مشروعك على GitHub.

---

## الطريقة الثانية: عبر GitHub Desktop (أسهل)

### الخطوة 1: تحميل GitHub Desktop

1. اذهب إلى https://desktop.github.com/
2. حمّل البرنامج وثبّته
3. سجل الدخول بحساب GitHub

### الخطوة 2: إضافة المشروع

1. افتح GitHub Desktop
2. اختر **File** → **Add Local Repository**
3. اختر مجلد المشروع `django_supabase_users`
4. إذا ظهرت رسالة "This directory does not appear to be a Git repository"، انقر على **create a repository**

### الخطوة 3: Commit الملفات

1. في GitHub Desktop، ستجد جميع الملفات في قائمة "Changes"
2. أدخل رسالة Commit في الأسفل (مثل: "Initial commit")
3. انقر على **Commit to main**

### الخطوة 4: النشر على GitHub

1. انقر على **Publish repository** في الأعلى
2. أدخل اسم المستودع ووصفه
3. اختر **Public** أو **Private**
4. انقر على **Publish repository**

✅ **تم بنجاح!** المشروع الآن على GitHub.

---

## تحديث المشروع بعد التعديلات

عندما تُجري تعديلات على المشروع وتريد رفعها:

### عبر Terminal/CMD:

```bash
git add .
git commit -m "وصف التعديلات"
git push
```

### عبر GitHub Desktop:

1. افتح GitHub Desktop
2. ستظهر التعديلات في "Changes"
3. أدخل رسالة Commit
4. انقر على **Commit to main**
5. انقر على **Push origin**

---

## نصائح مهمة

### ✅ افعل:

- ✅ استخدم رسائل commit واضحة ومفيدة
- ✅ تأكد من أن `.env` في `.gitignore`
- ✅ ارفع التعديلات بانتظام
- ✅ استخدم `.env.example` لمشاركة البنية دون البيانات الحساسة

### ❌ لا تفعل:

- ❌ لا ترفع ملف `.env` أبداً
- ❌ لا ترفع كلمات المرور أو مفاتيح API
- ❌ لا ترفع ملفات البيئة الافتراضية `venv/`
- ❌ لا ترفع ملفات قاعدة البيانات المحلية `db.sqlite3`

---

## التحقق من الملفات المرفوعة

للتأكد من أن الملفات الحساسة غير مرفوعة:

```bash
# عرض حالة Git
git status

# عرض الملفات المتجاهلة
git status --ignored
```

يجب أن ترى `.env` و `venv/` في قائمة الملفات المتجاهلة.

---

## استنساخ المشروع على جهاز آخر

إذا أردت العمل على المشروع من جهاز آخر:

```bash
# 1. استنساخ المشروع
git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git

# 2. الدخول إلى المجلد
cd YOUR_REPO

# 3. إنشاء ملف .env
# انسخ محتوى .env.example إلى .env وأضف بياناتك

# 4. إنشاء بيئة افتراضية
python3 -m venv venv

# 5. تفعيل البيئة
source venv/bin/activate  # Linux/Mac
# أو
venv\Scripts\activate  # Windows

# 6. تثبيت المتطلبات
pip install -r requirements.txt

# 7. تطبيق الهجرات
python manage.py migrate

# 8. تشغيل الخادم
python manage.py runserver
```

---

## مشاكل شائعة وحلولها

### المشكلة: "Permission denied (publickey)"

**الحل**: استخدم HTTPS بدلاً من SSH، أو أضف SSH key:
```bash
git remote set-url origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
```

### المشكلة: "Updates were rejected"

**الحل**: اسحب التحديثات أولاً:
```bash
git pull origin main
git push
```

### المشكلة: ملف .env ظهر على GitHub

**الحل**: احذفه من Git:
```bash
git rm --cached .env
git commit -m "Remove .env from repository"
git push
```

ثم غيّر جميع كلمات المرور والمفاتيح السرية فوراً!

---

## روابط مفيدة

- **وثائق Git**: https://git-scm.com/doc
- **وثائق GitHub**: https://docs.github.com/
- **GitHub Desktop**: https://desktop.github.com/
- **دليل Git بالعربية**: https://git-scm.com/book/ar/v2

---

**بالتوفيق! 🚀**
