# 🚀 دليل البدء السريع

## الخطوة 1️⃣: إعداد قاعدة البيانات في Supabase

1. افتح لوحة تحكم Supabase: https://supabase.com/dashboard
2. اختر مشروع `test1`
3. اذهب إلى **SQL Editor** من القائمة الجانبية
4. انقر على **New Query**
5. افتح ملف `supabase_setup.sql` وانسخ محتواه
6. الصق الكود في المحرر
7. انقر على **Run** ▶️

✅ **تم!** الآن لديك جدول `users` جاهز في قاعدة البيانات.

---

## الخطوة 2️⃣: الحصول على معلومات الاتصال

1. في لوحة تحكم Supabase، اذهب إلى **Settings** → **Database**
2. في قسم **Connection String**، اختر **URI**
3. انسخ الرابط وستجد فيه:
   - `db.XXXXX.supabase.co` ← هذا هو **DB_HOST**
   - `XXXXX` ← هذا هو **Project Reference ID**

---

## الخطوة 3️⃣: تحديث ملف .env

1. افتح ملف `.env` في المشروع
2. استبدل `YOUR_PROJECT_REF` برمز مشروعك `XXXXX`
3. احفظ الملف

مثال:
```env
DB_HOST=db.abcdefghijklmnop.supabase.co
```

---

## الخطوة 4️⃣: تشغيل المشروع محلياً

افتح Terminal/CMD في مجلد المشروع ونفذ:

```bash
# إنشاء بيئة افتراضية
python3 -m venv venv

# تفعيل البيئة (Linux/Mac)
source venv/bin/activate

# تفعيل البيئة (Windows)
venv\Scripts\activate

# تثبيت المتطلبات
pip install -r requirements.txt

# تطبيق الهجرات
python manage.py migrate

# إنشاء مستخدم إداري
python manage.py createsuperuser

# تشغيل الخادم
python manage.py runserver
```

---

## الخطوة 5️⃣: فتح الموقع

افتح المتصفح وانتقل إلى:
- **الموقع**: http://127.0.0.1:8000/
- **لوحة الإدارة**: http://127.0.0.1:8000/admin/

---

## الخطوة 6️⃣: الرفع على GitHub

### أ) إنشاء مستودع جديد

1. اذهب إلى https://github.com/new
2. أدخل اسم المستودع (مثل: `django-supabase-users`)
3. انقر على **Create repository**

### ب) رفع الكود

في Terminal/CMD، نفذ:

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git branch -M main
git push -u origin main
```

**استبدل**:
- `YOUR_USERNAME` باسم مستخدمك في GitHub
- `YOUR_REPO` باسم المستودع

---

## ✅ انتهى!

الآن لديك:
- ✅ موقع Django يعمل محلياً
- ✅ قاعدة بيانات في Supabase
- ✅ الكود مرفوع على GitHub

---

## 🆘 مشاكل شائعة

### لا يمكن الاتصال بقاعدة البيانات؟
- تحقق من `DB_HOST` في ملف `.env`
- تأكد من تنفيذ كود SQL في Supabase

### خطأ "table does not exist"؟
- نفذ `python manage.py migrate`

### نسيت كلمة مرور المدير؟
- نفذ `python manage.py createsuperuser` مرة أخرى

---

**للمزيد من التفاصيل، راجع ملف `README.md`**
